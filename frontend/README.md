# GridCool Frontend

Vite + React mission-control dashboard for GridCool.

## Local dev

```bash
npm install
npm run dev -- --port 5173
```

Optional env:
- `VITE_BACKEND_HTTP` (defaults to `http://localhost:8000`)
